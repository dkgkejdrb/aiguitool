// 프론트
import { InboxOutlined } from '@ant-design/icons';
import { Button, Form, Upload, Image, Input, Radio } from "antd";
import axios from 'axios';
import { useState } from 'react';
const { TextArea } = Input;

// middleware HOST 주소
// 나중에 정식 Host URL 필요함
const SUBMIT_URL = "http://localhost:3011/papagoLanguageDetection";

const PapagoLanguageDetection = () => {

    return(
        <></>
    );
}

export default PapagoLanguageDetection;